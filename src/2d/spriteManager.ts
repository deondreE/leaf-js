/**
 * This is an experimental class that will utilize a WebWorker to
 */
export default class SpriteManager {
  constructor() {
    //...
  }

  /**
   * When loading a sprite from an image asset we are
   */
  loadSprite() {} //Load sprite from some image asset.

  loadAseprite() {}

  loadPsd() {}
}
